Bugs/Issues:
- "create encounter" button permanently grayed out on iOS Safari
- tasks.html does not require an encounter or login


Tasks:
- add final interpretation to tasks (intensitiesAtReversal)

Refactors/Fixes:
- use on() rather than bind() in tasks, attach new div before binding events
- simplify config functions (always return valid config)
- break out staircasing code into TabCAT module
- automated testing
- make tabcat.css

UI:
- icon and favicon for TabCAT console
- Back button
- demo mode for tasks
- black out status bar on iPad
- Android fullscreen mode
- fix max height of inner div in inches, when possible, using something like:
  https://github.com/tombigel/detect-zoom



UI cleanup:
- make separate create_encounter.html page
